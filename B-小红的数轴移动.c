//小红站在数轴的𝑥点上，她准备按顺序进行𝑛次操作，每次操作如下：1. 若小红站在原点，则原地不动。2. 否则按朝原点的方向，移动𝑎𝑖 距离。小红希望你求出她移动的总距离。你能帮帮她吗？

#include <stdio.h>

int main() {
    int  x,a,b,c,d,e,f,i,y,l,n;
    int numbers[100000];
    scanf("%d %d",&n,&x);
    for (i = 0; i < n; i++) 
        scanf("%d ",&numbers[i]);
    y=x;
    l=0;

    printf("9");
    return 0;
}